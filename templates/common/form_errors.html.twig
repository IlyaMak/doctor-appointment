{% form_theme form _self %}

{% block form_errors %}
    {% if errors|length > 0 %}
        <div>
            {% for error in errors %}
                <div class="">{{ error.message }}</div>
            {% endfor %}
        </div>
    {% endif %}
{% endblock form_errors %}