{% block menu %}
    <div class="container">
        <div class="text-center row">
            <div class="col-sm-4">
                <a class="link-underline link-underline-opacity-0 link-underline-opacity-50-hover"
                    href="{{ path('schedule') }}">
                    <h6>{{ 'schedule_label'|trans }}</h6>
                </a>
            </div>
            <div class="col-sm-4">
                <a class="link-underline link-underline-opacity-0 link-underline-opacity-50-hover"
                    href="{{ path('set_working_hours_form') }}">
                    <h6>{{ 'set_working_hours_label'|trans }}</h6>
                </a>
            </div>
            <div class="col-sm-4">
                <a class="link-underline link-underline-opacity-0 link-underline-opacity-50-hover"
                    href="{{ path('delete_working_hours') }}">
                    <h6>{{ 'delete_working_hours_label'|trans }}</h6>
                </a>
            </div>
        </div>
    </div>
{% endblock %}