{% block menu %}
    <div class="ms-3 mt-3 d-flex">
        <a class="me-4 link-underline link-underline-opacity-0 link-underline-opacity-50-hover"
            href="{{ path('schedule') }}">
            <h4>Schedule</h4>
        </a>
        <a class="me-4 link-underline link-underline-opacity-0 link-underline-opacity-50-hover"
            href="{{ path('set_working_hours_form') }}">
            <h4>Set working hours</h4>
        </a>
        <a class="link-underline link-underline-opacity-0 link-underline-opacity-50-hover"
            href="{{ path('delete_working_hours') }}">
            <h4>Delete working hours</h4>
        </a>
    </div>
{% endblock %}